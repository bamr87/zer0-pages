/* zer0-pages — user-overrides JS hook (loaded by the theme when
 * `user_overrides: true`). Intentionally empty: the hook exists so the
 * theme's <script> tag resolves instead of 404ing. Add site JS here. */
